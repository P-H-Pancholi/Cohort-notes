----------------------Array.forEach(func)----------
forEach is a callback func & expects function as argument.

e.g.

const arr = [1,2,4]
arr.forEach(func) 
This will execute func on each element of given arr